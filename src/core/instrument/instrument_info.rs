pub struct InstrumentInfo {
    name: String,
}

impl InstrumentInfo {
    pub fn new(name: impl Into<String>) -> Self {
        Self { name: name.into() }
    }

    pub fn name(&self) -> &str {
        &self.name
    }
}
